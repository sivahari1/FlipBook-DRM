version: 1
frontend:
  phases:
    build:
      commands:
        - echo "🚀 Pure Static HTML Deployment"
        - mkdir -p dist
        - cp public/index.html dist/index.html
        - echo "✅ Static HTML copied successfully"
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'